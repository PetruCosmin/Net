<Window x:Class="Proiect_Licenta_NetworkCamApp.View.SetariView.DatabaseViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proiect_Licenta_NetworkCamApp.View.SetariView"
        mc:Ignorable="d"
        Title="DatabaseViewer" Height="450" Width="800">
    <Grid>
        <DataGrid Name="RecordDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                <DataGridTextColumn Header="Nume Fișier" Binding="{Binding FileName}" Width="*"/>
                <DataGridTextColumn Header="Tip" Binding="{Binding FileType}" Width="100"/>
                <DataGridTextColumn Header="Data Creării" Binding="{Binding CreationDate}" Width="150"/>
                <DataGridTextColumn Header="Dimensiune" Binding="{Binding FileSize}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
